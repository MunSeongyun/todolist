import "./App.css";
import { useState } from "react";

const App = () => {
  const toId = useState(0);

  let [list, setList] = useState([]);

  return (
    <div>
      <p>{list}</p>
      hjhjhjgghg
      >
        수정
      </button>
    </div>
  );
};

export default App;
